ssh-agent -s
ssh-add ~/.ssh/zc_private

ssh-agent -s
ssh-add ~/.ssh/id_rsa

!!!提交后记着恢复回来!!!
git config --global --replace-all user.name "zhaochong09"
git config --global --replace-all user.email "zhaochong09@meituan.com"